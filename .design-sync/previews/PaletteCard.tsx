import * as React from 'react';
import { PaletteCard } from 'sprout-brand-kit';

/** The locked six-colour palette specimen. */
export function Default() {
  return (
    <div style={{ width: 380 }}>
      <PaletteCard />
    </div>
  );
}
